# Validation report — `Receivables Tracker.pbip`

Generated by `tools/test_pbip.py` (exit code **0**) and a mutation run of the same suite.
Environment: Linux sandbox, Python 3.13, `openpyxl` 3.1.5, `jsonschema` 4.26.0.
No .NET runtime, no Analysis Services engine and no Power BI Desktop are available here —
see "What could not be tested".

## Result

```
==========================================================================
  PBIP test suite : Receivables Tracker.pbip
==========================================================================
    (workbook LPP as-of date recovered from column Z: 2026-09-12; the model accrues to TODAY = 2026-09-12)
  Structure + official JSON Schemas      11 JSON documents
  Project cross-references               5 pages
  TMDL parse + model integrity           8 tmdl files, 4 tables, 35 columns, 31 measures, 4 relationships
  DAX resolution & syntax                47 expressions, 15 constant-checked
  M pipeline simulation                  37 rows x 19 columns
  CSV vs Book1.xlsx                      703 cell comparisons
  DAX measures vs Excel results          610 value comparisons
  Report -> model binding                80 field references
--------------------------------------------------------------------------
  warnings: 6
  RESULT: PASS - no errors
--------------------------------------------------------------------------
```

## What was tested

| # | Group | Checks performed | Result |
| --- | --- | --- | --- |
| A | Structure & JSON Schema | 11 JSON documents parse; `.pbip`, `definition.pbir`, `definition.pbidataset`, both `item.config.json`, both `item.metadata.json`, both `localSettings.json`, `editorSettings.json` validated against **Microsoft's own Draft-7 schemas** (`microsoft/powerbi-desktop-samples/item-schemas`, cached in `.schemas/`) | pass |
| B | Cross references | `.pbip → .Report → .SemanticModel` path chain resolves; unique `logicalId`s; display names match folders; theme registered *and* present on disk; unique section and visual names; no visual overlaps the page bounds or another visual | pass |
| C/D | TMDL & model integrity | Every `.tmdl` file parsed by an indentation-aware parser into tables/columns/measures/partitions/relationships; `compatibilityLevel` matches `version.json`; every column typed in the M expression; `sortByColumn` targets exist; relationship endpoints exist; `PBI_SourceColumn` annotations match real CSV headers; partition mode is `import`; no duplicate names | pass |
| E | DAX | 47 expressions: bracket/brace/quote balance, every `'Table'[Column]` and bare `'Table'` resolves, every `[Measure]` resolves, no circular measure dependencies, no query keywords, and the business constants (18 %, 2 %, 2.25 %, 118 %, 92.65 %, 117.65 %, 25 %, 1.18 %, the 5-unit settle threshold, rounding levels) verified in 15 expressions | pass |
| F | Power Query | The M pipeline is replayed step by step in Python: base64 decode → `Csv.Document` → `PromoteHeaders` → `TransformColumnTypes`; every cell type-checked against its declared M type; the embedded CSV is byte-identical to `Data/receivables.csv`; `ReceivablesFromCsv` is proven to produce the *same* table as the embedded query | pass |
| G | Data fidelity | 703 cell-by-cell comparisons between `Data/receivables.csv` and `Book1.xlsx` (37 rows × 19 imported columns), full float precision | pass |
| H | Business rules | Every measure independently re-implemented in Python (with exact half-away-from-zero rounding) and compared with the values Excel itself cached: 610 comparisons across rows *and* the workbook's own TOTAL row | pass |
| I | Report binding | All 80 `Entity`/`Property` references in the 24 visuals resolve to a real column or measure of the right table | pass |

### Numbers reconcile exactly with the workbook

| Measure | Workbook (row 39) | Model |
| --- | ---: | ---: |
| Material Value | 15,130,598 | 15,130,598 |
| GST | 2,723,508 | 2,723,508 |
| Material Value incl. GST | 17,854,106 | 17,854,106 |
| TCS | 357,082 | 357,082 |
| TDS u/s 194(O) | 15,130.598 | 15,130.598 |
| Service Charge to MSTC | 401,717.3769 | 401,717.3769 |
| TDS u/s 194(H) | 6,808.7691 | 6,808.7691 |
| Net Service Charge | 394,908.6078 | 394,908.6078 |
| Service Charge to MSTC (Net) | 410,039 | 410,039 |
| GST TDS | 83,029 | 83,029 |
| Total Receivables in Cash | 17,746,099 | 17,746,099 |
| Security Deposit (Expected) | 3,782,654 | 3,782,654 |
| Security Deposit (Received) | 3,782,654 | 3,782,654 |
| Final Payment (Expected) | 13,935,473 | 13,935,473 |
| Final Payment (Received) | 13,646,312 | 13,646,312 |
| LPP Expected | 27,976.148 | 27,976.148 |
| Total Received | 17,445,472 | 17,445,472 |
| Outstanding | 300,627 | 300,627 |

`LPP Expected` matches because the workbook was last calculated on 2026‑09‑12, the same day
the suite ran; the test recovers that as-of date from the sheet's own unpaid rows instead of
assuming it. The model recomputes to `TODAY()`, so this figure grows weekly on the two
unpaid lots.

## Mutation testing — does the suite actually catch anything?

18 defects were injected into the built artifacts, one at a time, and the suite re-run.
**18/18 were detected**, and each file was restored afterwards:

| Injected defect | Detected by |
| --- | --- |
| DAX references a non-existent column | E. DAX |
| DAX references a non-existent measure | E. DAX |
| DAX references a non-existent table (`COUNTROWS('Receivable')`) | E. DAX |
| Unbalanced parenthesis in a measure | E. DAX |
| Wrong rate (`0.18` → `0.17`) | E. DAX constants |
| Dropped `ROUND()` from GST | E. DAX constants |
| Invalid `dataType: datetime` | C/D. model integrity |
| Relationship to a missing column | C/D. model integrity |
| Partition mode `directQuery` | C/D. model integrity |
| Duplicate column name | C/D. model integrity |
| M expression stops typing a column | C/D. model integrity |
| Visual binds a missing measure | I. report binding |
| Duplicate visual name on a page | B. cross references |
| `.pbip` points at a missing folder | B. cross references |
| `definition.pbir` version not allowed by the schema | A. JSON Schema |
| Embedded CSV and `Data/receivables.csv` disagree | F. Power Query |
| One corrupted CSV cell | F. Power Query |

This matters because the first version of the suite scored 12/15: it silently missed a bad
table reference and a wrong DAX constant. Both gaps were closed before the run above.

## Postmortem — the bug Power BI Desktop caught and this suite did not

Opening the project in Power BI Desktop (August 2026, 2.157.1354.0) failed with:

> `ReportDefinition: Required artifact is missing in '...\definition.pbir'`
> `InnerException0: RequiredArtifactMissing: Path: definition.pbir`
> `InnerException1: PBIProjectSchemaValidator.IsJsonLegallyEmpty(...)`

**Cause.** `definition.pbir` was written as `"version": "1.0"` while its `$schema` pointed
at `definitionProperties/2.0.0`. Desktop's `PBIProjectSchemaValidator` tries each schema
variant in turn and, when none matches, reports the artifact as missing. The 1.0.0 schema
requires the `$schema` URL to match `definitionProperties/1.[0-9]+.[0-9]+`, so the file
matched neither variant. The documented, current pairing is `"version": "4.0"` with
`definitionProperties/2.0.0` — and per Microsoft's docs, version 4.0 still allows the
PBIR-Legacy `report.json` this project uses.

**Why the suite missed it.** It validated against `microsoft/powerbi-desktop-samples/
item-schemas` (published June 2023). Those schemas have no `$schema` property and set
`additionalProperties: false` — so they flagged `$schema` as *unexpected*, and the suite
downgraded that to a benign warning with the note "written by current Desktop; kept
intentionally". That warning was the bug. The authoritative schemas live in
**`microsoft/json-schemas`**, and there `$schema` is **required** in every item file.

**Five files had the same defect**, not just `definition.pbir`:

| File | Was | Now |
| --- | --- | --- |
| `Receivables Tracker.pbip` | no `$schema` | `fabric/pbip/pbipProperties/1.0.0` |
| `…Report/definition.pbir` | `$schema` 2.0.0 + `version` 1.0 (mismatch) | `$schema` 2.0.0 + `version` **4.0** |
| `…Report/.pbi/localSettings.json` | `{"version":"1.0"}` | `fabric/item/report/localSettings/1.0.0` |
| `…SemanticModel/definition.pbidataset` | no `$schema` | `fabric/item/semanticModel/definitionProperties/1.0.0` |
| `…SemanticModel/.pbi/editorSettings.json` | `{"version":"1.0"}` | `fabric/item/semanticModel/editorSettings/1.0.0` |

**Fix.** `.schemas/authoritative/` now holds the `microsoft/json-schemas` definitions, the
suite validates against those and fails on any mismatch, the `$schema`-is-unexpected
warning is gone, and a new check asserts that `definition.pbir`'s `$schema` family agrees
with its `version`. The mutation run was extended to 24 cases, including a byte-for-byte
reproduction of the Desktop failure; **24/24 are now detected**.

## Remaining warnings (all benign, kept on purpose)

1. Three tables (`Buyer`, `Receipt Date`, `Invoice Date`) are DAX calculated tables, so no
   partition is emitted for them.
3. Four names exist as both a column and a measure (`Material Value`,
   `Security Deposit (Received)`, `Final Payment (Received)`, `LPP Received`) so the report
   reads like the spreadsheet. Legal in the Tabular object model; `Material Value` etc. in a
   visual is always the measure, never the column.
4. 32 of the 47 expressions are not in the constant table (date parsing, counts, `SUM`s and
   the date tables — nothing to constant-check).

## What could **not** be tested here

* **DAX is not executed.** No Analysis Services / VertiPaq engine runs on Linux, and the
  .NET SDK is unreachable from this sandbox, so measures were verified by static analysis
  plus an independent Python re-implementation — not by running them.
* **Power Query is not executed.** The M pipeline was replayed by hand in Python.
  `Binary.FromText`, `Csv.Document`, `Table.PromoteHeaders` and `Table.TransformColumnTypes`
  have exact, documented semantics; the embedded data was chosen precisely so that nothing
  depends on a folder path or on privacy-level prompts.
* **The report was not rendered.** Visual types, query-ref shape and layout follow the
  PBIR-Legacy conventions, and every field binding resolves, but only Power BI Desktop can
  confirm the visuals draw.
* **`ReceivablesFromCsv`** (the refresh path that reads `Data/receivables.csv`) resolves the
  folder from `#section[Section1][…][Meta][ConceptualRelativePath]`. It is proven to produce
  the same table as the embedded query when given the same bytes, but that metadata trick
  could not be executed here — the embedded query is the default for exactly that reason.

## Source-data issues found

| Row(s) | Issue | Handling |
| --- | --- | --- |
| 25 | `Quantity` is `8.2`, not an integer | `Quantity` typed decimal (`#,##0.###`) |
| 27, 34 | No final payment; the receipt date is the number `0` | Treated as blank, matching the sheet's own `OR(Y="",Y=0)` test |
| 25, 27, 34, 37, 38 | No Invoice No. / SAP Document / Invoice Date | Flagged by `[Lots Missing Invoice Details]` and listed on the Data Quality page |
| 7, 12 | Fractional final payments (`2582447.09429161`, `3573618.90570839`) | `Final Payment (Received)` typed decimal, stored at full precision |
| V and Y | Both headers read "Date of Receipt" | Renamed `SD Receipt Date (Text)` / `FP Receipt Date (Text)` |
| 39, 40 | Row 39 is a TOTAL row; row 40 holds a stray `=+H39` block | Neither is imported (the model aggregates with DAX); row 40's `Total Receivables` of 17,718,119 is stale because it omits the penalty, whereas the correct figure is 17,746,099 |
